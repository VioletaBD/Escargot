<nav class="navbar navbar-light p-1 mb-5 fixed-top mx-lg-auto">
	<div class="container-fluid">
		<div class="col-start">
			<div class="row">
				<div class="navbar-brand">
					<a class="navbar" href="{{ path('app_index')}}">
						<img src="{{ asset('build/images/recycle.png') }}" alt="" width="100" height="100" class="img-fluid d-inline-block">
						<h3 class="display-4">Escargot en action</h3>
					</a>
				</div>
			</div>
			<div class="row">
				<h3 class="title">De petits mouvements peuvent avoir de grands impacts !</h3>
			</div>
		</div>
		<div class="logout">
			<a class="navbar" href="{{ path('app_login')}}">
				{% if app.user %}
					<h5>{{ app.user.name }}</h>
					<a class="btn btn-outline-success" href="{{ path('app_logout') }}">
						Se déconnecter</a>
				{% else %}
					<a class="btn btn-success" href="{{ path('app_login') }}">
						Se connecter</a>
				{% endif %}
			</a>
			<a class="navbar" href="{{ path('app_register')}}">
				<button type="button" class="btn btn-outline-success">Inscription</button>
			</a>
		</div>
	</div>
</nav>
