{# <nav class="navbar navbar-primary p-1 mb-1 sticky-top mx-lg-auto">
	<div class="container-fluid">
		<div class="col-start">
			<div class="row">
				<div class="navbar-brand">
					<a class="navbar" href="{{ path('app_index')}}">
						<img src="{{ asset('build/images/recycle.png') }}" alt="Logo" width="60" height="60" class="img-fluid d-inline-block">
						<h3 class="text-decoration-none">Escargot en action</h3>
					</a>
				</div>
			</div>
		</div>
		<div class="logout">
			<a class="navbar" href="{{ path('app_login')}}">
				{% if app.user %}
					<h5>{{ app.user.name }}</h>
					<a class="btn btn-outline-success" href="{{ path('app_logout') }}">
						Se déconnecter</a>
				{% else %}
					<a class="btn btn-success" href="{{ path('app_login') }}">
						Participate a une sortie</a>
				{% endif %}
			</a>
			<a class="btn btn-success" href="{{ path('app_index') }}">
						Voir les prochain sortie</a>
			</a>
			
		</div>
	</div>
</nav> #}

<nav class="navbar navbar-expand-lg p-2 bg-secondary sticky-top">
	<div class="container-fluid">

		<div class="d-flex align-items-center">
			<img src="{{ asset('build/images/recycle.png') }}" width="50px" height="50px" class="img-fluid ms-2" alt="Logo">
			<a class="navbar-brand px-2" href="{{ path('app_index') }}">
				<h1 class="fs-3 text-light text-wrap ps-2">Escargot en action</h1>
			</a>

		</div>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
			<div class="navbar-nav">
					<a class="nav-link p-2 text-light" href="{{ path('app_index') }}">Voir les prochain sortie</a>
					<a class="nav-link p-2 text-light me-2" href="{{ path('app_login') }}">Participate a une sortie</a>
			</div>
		</div>
	</nav>
