vich_uploader:
    db_driver: orm

    metadata:
        type: attribute

    mappings:
        outing_file:
            uri_prefix: /uploads/outings
            upload_destination: '%kernel.project_dir%/public/uploads/outings'
            namer: Vich\UploaderBundle\Naming\SmartUniqueNamer
